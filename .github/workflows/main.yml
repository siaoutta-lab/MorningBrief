name: MorningBrief

on:
  schedule:
    - cron: '0 23 * * *'  # UTC时间23点 = 曼谷/北京早上6点
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "MorningBrief system is running"
